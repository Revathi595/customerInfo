import { ValidateNumbersDirective } from './validate-numbers.directive';

describe('ValidateNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
